---
# Source: safeline/templates/nginx/nginx-pvc.yaml
kind: PersistentVolumeClaim
apiVersion: v1
metadata:
  name: release-name-safeline-nginx
  annotations:
    helm.sh/resource-policy: keep
  labels:
    heritage: Helm
    release: release-name
    chart: safeline
    app: "safeline"
    component: nginx
spec:
  accessModes: 
    - ReadWriteMany
  resources:
    requests:
      storage: 1Gi
